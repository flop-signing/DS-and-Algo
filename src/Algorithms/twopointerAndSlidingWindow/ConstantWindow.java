package Algorithms.twopointerAndSlidingWindow;


// Suppose given an array and [-1, 2, 3, 3, 4, 5, -1]; and k=4
// Find the maximum sum that can obtain picking up k element consecutively


public class ConstantWindow {

/*

    int k; // size of the window
    int l=0;
    int r=k-1;



    while(r<n-1){
        sum = sum-arr[l];
        l++;
        r++;
        sum=sum+arr[r];

        maxSum=max(maxSum,sum);
    }


    */
}
