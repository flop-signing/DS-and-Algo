package Algorithms.twopointerAndSlidingWindow;

public class LongestCommonSubstringWithoutRepeatingCharacter {

/*    // pseudo code

    for(int i=0;i<n;i++){
        hash[250]={0}; /// stored the ASCII value of each character
    for(int j=i;j<n;j++){
        if(hash[s[j]]==1){
            len=j-i+1;
            // put the logic of updating max each time to get the actual value of the max.
            hash[j]=1;
 */

    // Complexity is O(N)

    // The optimize solutions

    // In case of finding substring the suitable approach is the two pointer and the sliding window method

    /*

    while(r<n){





    }


     */

}
